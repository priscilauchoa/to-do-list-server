const allTasks = () => {
    //get data 
    //to do
    // res.json(tasks);
};
const byId = () => {
    //get data 
    //to do
    // res.json(tasks);
};
const editTask = () => {
    //edit data
    //to do
    // res.json(tasks);
};
const removeTask = () => {
    //remove data 
    //to do
};

