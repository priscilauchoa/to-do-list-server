export const allTasks = () => {
    //get data 
    //to do
    // res.json(tasks);
};
export const byId = () => {
    //get data 
    //to do
    // res.json(tasks);
};
export const editTask = () => {
    //edit data
    //to do
    // res.json(tasks);
};
export const removeTask = () => {
    //remove data 
    //to do
};

